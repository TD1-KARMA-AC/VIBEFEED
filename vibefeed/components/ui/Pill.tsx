import { View, Text, StyleSheet } from 'react-native';

export default function Pill() {
  return <View style={styles.pill}><Text style={styles.text} /></View>;
}

const styles = StyleSheet.create({
  pill: {},
  text: {},
});
